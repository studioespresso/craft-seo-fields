# robots.txt for {{ siteUrl }}

{% switch getenv('ENVIRONMENT') %}

{% case "live" or "production" %}

User-agent: *
Disallow: /cpresources/
Disallow: /vendor/
Disallow: /.env

{% case "local" or "dev" %}

# local - disallow all

User-agent: *
Disallow: /

{% default %}

User-agent: *
Disallow: /cpresources/
Disallow: /vendor/
Disallow: /.env

{% endswitch %}