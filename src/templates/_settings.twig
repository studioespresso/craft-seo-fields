{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: 'Plugin sidebar label'|t('seo-fields'),
    id: 'pluginLabel',
    name: 'pluginLabel',
    size: 25,
    value: settings['pluginLabel']})
}}

{{ forms.textField({
    label: 'Title seperator'|t('seo-fields'),
    instructions: 'A character used to seperate your `<title>` from the site name'|t('seo-fields'),
    id: 'titleSeperator',
    name: 'titleSeperator',
    size: 3,
    value: settings['titleSeperator']})
}}

<hr>

<h2>{{ "Redirects"|t }}</h2>
{{ forms.lightswitchField({
    label: 'Should redirects be created automatically when uri\'s change?'|t('seo-fields'),
    id: 'createRedirectForUriChange',
    name: 'createRedirectForUriChange',
    on: settings.createRedirectForUriChange
}) }}
