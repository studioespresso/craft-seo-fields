{% tag 'a' with {
    href: actionUrl('seo-fields/not-found/clear-all'),
    class: 'btn',
    onclick: 'return confirm("Are you sure you want to remove all tracked 404")',
} %}
    {{ 'Clear all'|t('seo-fields') }}
{% endtag %}
